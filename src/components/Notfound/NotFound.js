import React from "react";

const NotFound = () => {
  return (
    <div
      style={{
        color: "red",
        weight: "bold",
        fontSize: "40px",
        textAlign: "center",
        marginTop: "80px",
      }}
    >
      404 not found!
    </div>
  );
};

export default NotFound;
